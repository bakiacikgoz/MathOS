/** @jsxImportSource @opentui/solid */
import{theme}from"../theme.ts";export function publicationCommand(args:string[]){const action=args[0]??"inspect";if(!["build","inspect"].includes(action))throw new Error("PUBLICATION_ACTION_UNKNOWN");return{action,args:args.slice(1)}}export function publicationSnapshot(input:{sections:string[];lossReport:string[]}){return{title:"PUBLICATION STUDIO",watermark:input.lossReport.length?"INCOMPLETE PUBLICATION":"",...input}}export function PublicationView(props:{snapshot:ReturnType<typeof publicationSnapshot>}){return <box flexDirection="column"><text fg={theme.accent}>{props.snapshot.title}</text><text fg={theme.warning}>{props.snapshot.watermark}</text></box>}
