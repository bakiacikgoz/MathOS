/** @jsxImportSource @opentui/solid */
import{theme}from"../theme.ts";export function reviewCommand(args:string[]){const action=args[0]??"create";if(!["create","finding-add","finding-resolve","attest","plan"].includes(action))throw new Error("REVIEW_ACTION_UNKNOWN");return{action,args:args.slice(1)}}export function reviewSnapshot(input:{hash:string;findings:any[];status:string}){return{title:"SEMANTIC REVIEW",...input}}export function ReviewView(props:{snapshot:ReturnType<typeof reviewSnapshot>}){return <box flexDirection="column"><text fg={theme.accent}>{props.snapshot.title}</text><text fg={theme.text}>{`HASH ${props.snapshot.hash}`}</text></box>}
