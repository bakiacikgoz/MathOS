import{theme}from"../theme.ts";export function atlasTextCommand(args:string[]){const action=args[0]??"snapshot";if(!["snapshot","open","export","impact","critical-path"].includes(action))throw new Error("ATLAS_ACTION_UNKNOWN");return{action,args:args.slice(1)}}export function atlasSessionView(session:{url:string;token:string;stop:()=>void}){return{url:session.url,token:`***${session.token.slice(-4)}`,authority:"READ_ONLY" as const,stop:session.stop}}export function AtlasView(props:{text:string}){return <box flexGrow={1}padding={1}flexDirection="column"><text fg={theme.accent}>THEOREM ATLAS</text><text fg={theme.warning}>READ ONLY</text><text fg={theme.text}>{props.text}</text></box>}
/** @jsxImportSource @opentui/solid */
