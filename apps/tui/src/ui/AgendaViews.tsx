/** @jsxImportSource @opentui/solid */
import{theme}from"../theme.ts";export function agendaCommand(args:string[]){const action=args[0]??"list";if(!["list","add","next","complete","home"].includes(action))throw new Error("AGENDA_ACTION_UNKNOWN");return{action,args:args.slice(1)}}export function agendaSnapshot(items:any[]){return{title:"RESEARCH AGENDA",items}}export function AgendaView(props:{snapshot:ReturnType<typeof agendaSnapshot>}){return <box flexDirection="column"><text fg={theme.accent}>{props.snapshot.title}</text><text fg={theme.text}>{`${props.snapshot.items.length} items`}</text></box>}
