import { describe, expect, test } from "bun:test"
import { createAtlasAppModel, renderAtlasHtml } from "./App.tsx"
const snapshot:any={schemaVersion:"atlas-snapshot-v1",workspaceId:"W",eventSequence:1,hash:"h",nodes:[{id:"C1",kind:"CLAIM",label:"Claim",status:"DRAFT"},{id:"B1",kind:"BLOCKER",label:"Block",status:null}],edges:[{id:"e",kind:"BLOCKS",from:"B1",to:"C1"}],coverage:{claimsTotal:1,verified:0}}
describe("Atlas app",()=>{test("filters, selects and exposes coverage/legend/critical path",()=>{const model=createAtlasAppModel(snapshot);expect(model.filter("CLAIM").map(n=>n.id)).toEqual(["C1"]);expect(model.select("C1")?.label).toBe("Claim");expect(model.criticalPath("B1")).toEqual(["B1","C1"]);const html=renderAtlasHtml(snapshot);expect(html).toContain("Status legend");expect(html).toContain("Coverage");expect(html).toContain("aria-label")});test("supports loading empty and error states with reduced motion",()=>{expect(renderAtlasHtml(null)).toContain("Loading");expect(renderAtlasHtml({...snapshot,nodes:[],edges:[]})).toContain("No theorem nodes");expect(renderAtlasHtml(snapshot,{error:"broken",reducedMotion:true})).toContain("broken");expect(renderAtlasHtml(snapshot,{error:"broken",reducedMotion:true})).toContain("data-reduced-motion=\"true\"")})})
