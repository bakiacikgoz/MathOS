import{validateMathWidget}from"@mathos/graph";import type{MathWidgetSpec}from"@mathos/domain";const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));export function renderMathWidget(spec:MathWidgetSpec){const value=validateMathWidget(spec);return`<figure tabindex="0" aria-label="${esc(value.textFallback)}"><figcaption>${esc(spec.title)} · ${spec.trustLabel}</figcaption><pre>${esc(value.textFallback)}</pre></figure>`}
