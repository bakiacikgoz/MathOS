import{describe,expect,test}from"bun:test";import{renderMathWidget}from"./MathWidget.tsx";describe("MathWidget",()=>{test("renders focusable trust-labelled text/table fallback without scripts",()=>{const html=renderMathWidget({schemaVersion:"math-widget-v1",id:"w",kind:"FINITE_TABLE",title:"Values",data:{columns:["x"],rows:[[1]]},sourceEntityIds:["C1"],trustLabel:"STRUCTURAL"} as any);expect(html).toContain('tabindex="0"');expect(html).toContain("STRUCTURAL");expect(html).toContain("Values");expect(html).not.toContain("<script")})})
