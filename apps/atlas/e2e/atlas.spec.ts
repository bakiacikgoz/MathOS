import{describe,expect,test}from"bun:test";import{renderAtlasHtml}from"../src/App.tsx";describe("atlas accessibility smoke",()=>{test("keyboard-focusable textual graph alternative",()=>{const html=renderAtlasHtml({schemaVersion:"atlas-snapshot-v1",workspaceId:"W",eventSequence:1,hash:"h",nodes:[{id:"C",kind:"CLAIM",label:"Claim",status:"DRAFT"}],edges:[],coverage:{}} as any);expect(html).toContain('tabindex="0"');expect(html).toContain("Graph text alternative")})})
