# MathOS for VS Code

This companion displays MathOS workspace status and invokes explicit actions through the local, versioned bridge. Untrusted workspaces remain read-only. Credentials belong in the MathOS secret store, never VS Code settings.
