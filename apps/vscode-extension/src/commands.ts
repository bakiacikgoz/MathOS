const names=["openClaim","sendLean","inspectPremises","startPortfolio","showAlignment","openAtlas","showNextActions","showModelProviders","selectModelProfile","refreshProviderStatus","showProviderQuota"]as const
type Name=typeof names[number]
export class MathOSCommands{constructor(private readonly d:{trusted:boolean;send:(message:unknown)=>Promise<unknown>}){}prepare(name:Name,selection:string){if(!names.includes(name))throw new Error("COMMAND_UNKNOWN");if(!this.d.trusted&&["sendLean","startPortfolio","selectModelProfile"].includes(name))throw new Error("WORKSPACE_UNTRUSTED");if(Buffer.byteLength(selection)>100_000)throw new Error("SELECTION_TOO_LARGE");return{name,selection}}async invoke(name:Name,params:unknown,signal?:AbortSignal){if(signal?.aborted)throw new Error("COMMAND_CANCELLED");return await this.d.send({method:`command.${name}`,params})}registeredEvents(){return["onCommand"]}capabilities(){return["claims.read","graph.read","commands.invoke","providers.read"]}}
