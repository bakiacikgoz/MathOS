import{mkdtempSync,writeFileSync}from"node:fs";import{tmpdir}from"node:os";import{join}from"node:path";import{exportCapsuleArchive,verifyCapsuleArchive}from"../packages/core/src/capsule-archive.ts";const root=mkdtempSync(join(tmpdir(),"mathos-capsule-smoke-")),file=join(root,"proof.lean"),out=join(root,"smoke.mathos-capsule");writeFileSync(file,"theorem smoke : True := by trivial\n");exportCapsuleArchive({root,output:out,paths:["proof.lean"]});const report=verifyCapsuleArchive(out);if(!report.ok)throw new Error(report.errors.join(","));console.log("capsule roundtrip PASS")
