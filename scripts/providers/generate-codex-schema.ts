import{writeFileSync}from"node:fs";import{resolve}from"node:path";import{validateCodexSchema}from"../../packages/models/src/protocols/codex-app-server.ts"
const executable=process.argv[2]??Bun.which("codex");if(!executable)throw new Error("CODEX_CLIENT_MISSING");const output=resolve(process.argv[3]??"artifacts/codex-app-server-schema.json");const child=Bun.spawn([executable,"app-server","generate-json-schema"],{stdin:"ignore",stdout:"pipe",stderr:"inherit"});const text=await new Response(child.stdout).text();if(await child.exited!==0)throw new Error("CODEX_SCHEMA_GENERATION_FAILED");const schema=JSON.parse(text);validateCodexSchema(schema);writeFileSync(output,`${JSON.stringify(schema,null,2)}\n`,{mode:0o600});console.log(JSON.stringify({output,validated:true}))
