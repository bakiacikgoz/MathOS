# Research brief

Objective: prove the finite sum identity `sum (2k + 1) = n²` for natural `n`.

Initial status: `CANDIDATE_CONJECTURE`.

Acceptance: current formal statement is human-approved for fidelity and a proof is accepted by the pinned local Lean kernel with no forbidden constructs or unapproved axioms. Finite computation remains `COMPUTATIONAL_EVIDENCE`.
