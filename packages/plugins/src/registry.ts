export class GovernedPluginRegistry{private rows=new Map<string,{manifest:any;hash:string;status:"DISABLED"|"ENABLED"|"APPROVAL_REQUIRED"}>();private auditRows:any[]=[];install(manifest:any,hash:string){this.rows.set(manifest.id,{manifest,hash,status:"DISABLED"})}enable(id:string,hash:string,actor:string){const r=this.must(id);if(!actor)throw new Error("PLUGIN_APPROVAL_ACTOR_REQUIRED");if(r.hash!==hash)throw new Error("PLUGIN_MANIFEST_HASH_MISMATCH");r.status="ENABLED";this.auditRows.push({id,action:"ENABLED",actor})}disable(id:string,actor:string){this.must(id).status="DISABLED";this.auditRows.push({id,action:"DISABLED",actor})}status(id:string){return this.must(id).status}require(id:string,schema:string){const r=this.must(id);if(r.status!=="ENABLED")throw new Error("PLUGIN_DISABLED");if(!r.manifest.supportedSchemaVersions.includes(schema))throw new Error("PLUGIN_SCHEMA_UNSUPPORTED");return r.manifest}permissionDenied(id:string,capability:string){this.auditRows.push({id,action:"PERMISSION_DENIED",capability})}audit(){return[...this.auditRows]}private must(id:string){const r=this.rows.get(id);if(!r)throw new Error("PLUGIN_NOT_FOUND");return r}}
