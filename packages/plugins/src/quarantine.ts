export class PluginQuarantine{private events=new Map<string,Array<{kind:string;at:string}>>();private enabled=new Set<string>();record(id:string,kind:"SECURITY"|"PROTOCOL"|"CRASH"){this.push(id,kind)}recordSuccess(id:string){this.push(id,"SUCCESS")}status(id:string){return this.failures(id)>=3&&!this.enabled.has(id)?"QUARANTINED":"ENABLED"}reenable(id:string,actor:string){if(!actor)throw new Error("PLUGIN_REENABLE_ACTOR_REQUIRED");this.enabled.add(id);this.push(id,`REENABLED:${actor}`)}history(id:string){return[...(this.events.get(id)??[])]}private failures(id:string){return(this.events.get(id)??[]).filter(e=>["SECURITY","PROTOCOL","CRASH"].includes(e.kind)).length}private push(id:string,kind:string){const rows=this.events.get(id)??[];rows.push({kind,at:new Date().toISOString()});this.events.set(id,rows)}}
