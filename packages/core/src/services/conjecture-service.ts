import type{ConjectureDraft,ConjectureProposal}from"@mathos/domain";import{deterministicConjectures}from"../conjecture-generators/deterministic.ts";import{parseModelConjectures}from"../conjecture-generators/model.ts";type Opt={workspaceId:string;branchId:string;contextRevisionId:string;maxProposals:number;insert?:(row:ConjectureProposal)=>void;now?:()=>string};export class ConjectureService{private sequence=0;private seen=new Set<string>();constructor(private readonly o:Opt){if(!o.contextRevisionId.trim())throw new Error("CONJECTURE_CONTEXT_REQUIRED");if(o.maxProposals<1)throw new Error("CONJECTURE_LIMIT_INVALID")}generateDeterministic(input:Parameters<typeof deterministicConjectures>[0]){return this.materialize(deterministicConjectures(input),"PATTERN")}generateModel(values:Record<string,unknown>[]){return this.materialize(parseModelConjectures(values,this.o.maxProposals),"MODEL")}private materialize(drafts:ConjectureDraft[],generator:ConjectureProposal["generator"]){const out:ConjectureProposal[]=[];for(const draft of drafts){const key=draft.naturalStatement.trim().toLowerCase().replace(/\s+/g," ");if(this.seen.has(key))continue;this.seen.add(key);const row:ConjectureProposal={id:`CP-${++this.sequence}`,workspaceId:this.o.workspaceId,branchId:this.o.branchId,generator,sourceEntityIds:draft.sourceEntityIds??[],naturalStatement:draft.naturalStatement,rationale:draft.rationale,contextRevisionId:this.o.contextRevisionId,status:"PROPOSED",formalCandidateId:null,createdClaimId:null,createdAt:this.o.now?.()??new Date().toISOString(),decidedAt:null};this.o.insert?.(row);out.push(row);if(out.length>=this.o.maxProposals)break}return out}}
