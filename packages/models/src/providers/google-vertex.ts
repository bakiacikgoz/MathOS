import{OpenAIChatTransport}from"../transports/openai-chat.ts";import type{HttpTransportConfig}from"../transports/types.ts";export function vertexOpenAIBaseUrl(projectId:string,location:string){if(!/^[a-z][a-z0-9-]{3,62}$/.test(projectId)||!/^[a-z0-9-]+$/.test(location))throw new Error("VERTEX_LOCATION_INVALID");return`https://${location}-aiplatform.googleapis.com/v1beta1/projects/${projectId}/locations/${location}/endpoints/openapi`};export const createGoogleVertexTransport=(config:HttpTransportConfig)=>new OpenAIChatTransport(config)
