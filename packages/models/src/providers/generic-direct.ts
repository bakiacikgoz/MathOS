import{InvalidStructuredResponse}from"../errors.ts";import{extractJson}from"../json.ts";import type{ModelProvider,ModelRequest,ModelResponse,StructuredModelRequest}from"../types.ts";import type{NormalizedTransport}from"../transports/types.ts"
export class GenericDirectProvider implements ModelProvider{readonly capabilities={structuredOutput:true,toolCalling:false,reasoning:true,streaming:false,vision:false};constructor(readonly id:string,readonly model:string,private readonly transport:NormalizedTransport){}toJSON(){return{id:this.id,model:this.model,capabilities:this.capabilities,protocol:this.transport.protocol}}async generate(request:ModelRequest):Promise<ModelResponse>{const response=await this.transport.generate(request);return{text:response.text,provider:this.id,model:this.model}}async generateStructured<T>(request:StructuredModelRequest<T>):Promise<T>{const schema=request.responseSchema??{name:request.schemaName,jsonSchema:{type:"object",additionalProperties:true}},first=await this.generate({...request,responseSchema:schema});try{return request.parse(extractJson(first.text))}catch(error){const repair=await this.generate({...request,responseSchema:schema,messages:[...request.messages,{role:"assistant",content:first.text},{role:"user",content:`Return only repaired JSON for ${request.schemaName}: ${error instanceof Error?error.message:"invalid"}`}]});try{return request.parse(extractJson(repair.text))}catch{throw new InvalidStructuredResponse("The model returned an invalid structured response after one repair attempt.")}}}}
export function assertLiveUsageAccepted(input:{live?:boolean;acceptUsage?:boolean},billingClass:string):void{if(input.live&&billingClass==="payg"&&!input.acceptUsage)throw new Error("LIVE_USAGE_ACCEPTANCE_REQUIRED")}
