import { providerCatalog } from "./catalog.ts"
export interface ProviderPolicyResult{descriptorId:string;allowed:boolean;code:"PROVIDER_ALLOWED"|"PROVIDER_TERMS_RESTRICTED"|"PROVIDER_RETIRED";warnings:string[];remediation:string|null}
export function evaluateProviderPolicy(id:string,now=new Date()):ProviderPolicyResult{const d=providerCatalog.get(id);if(!d)throw new Error(`PROVIDER_DESCRIPTOR_NOT_FOUND: ${id}`);const reviewed=new Date(`${d.terms.lastReviewedAt}T00:00:00Z`),warnings=now.getTime()-reviewed.getTime()>90*86400000?["PROVIDER_POLICY_REVIEW_STALE"]:[];if(d.terms.policy==="RETIRED")return{descriptorId:id,allowed:false,code:"PROVIDER_RETIRED",warnings,remediation:id==="qwen-portal-oauth-legacy"?"Use Alibaba plan/PAYG or OpenRouter.":null};if(["PROHIBITED_THIRD_PARTY","REQUIRES_VENDOR_APPROVAL","TERMS_REVIEW_REQUIRED"].includes(d.terms.policy))return{descriptorId:id,allowed:false,code:"PROVIDER_TERMS_RESTRICTED",warnings,remediation:id==="zai-coding-plan"?"zai-payg":id==="google-antigravity-consumer"?"Use Gemini API, Vertex AI, or an officially permitted Enterprise Agent Platform API.":null};return{descriptorId:id,allowed:true,code:"PROVIDER_ALLOWED",warnings,remediation:null}}
