import type{ProviderConnectionState}from"../catalog/types.ts";import{runProviderHealthProbe,type ProviderHealthProbe,type ProviderProbeInput}from"./probe.ts"
export interface ProviderHealthReport{state:ProviderConnectionState;layers:ProviderHealthProbe;checkedAt:string}
export class ProviderHealthService{constructor(private readonly now=()=>new Date()){}async inspect(input:ProviderProbeInput):Promise<ProviderHealthReport>{const layers=await runProviderHealthProbe(input);let state:ProviderConnectionState=!layers.configuration.ok?"UNCONFIGURED":layers.authentication.ok===false?"LOGIN_REQUIRED":layers.transport.ok===false?"DEGRADED":layers.model.ok===false?"MODEL_UNAVAILABLE":layers.quota.detail==="exhausted"?"QUOTA_EXHAUSTED":"CONNECTED";return{state,layers,checkedAt:this.now().toISOString()}}}
