import{validateMathWidget}from"@mathos/graph";import type{MathWidgetSpec}from"@mathos/domain";const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));export function renderWidgetHtml(spec:MathWidgetSpec){const checked=validateMathWidget(spec);return`<figure data-widget="${spec.kind}"><figcaption>${esc(spec.title)} · ${spec.trustLabel}</figcaption><pre>${esc(checked.textFallback)}</pre><small>Sources: ${spec.sourceEntityIds.map(esc).join(", ")}</small></figure>`}
