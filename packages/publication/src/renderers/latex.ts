import{validateMathWidget}from"@mathos/graph";import type{MathWidgetSpec}from"@mathos/domain";const esc=(s:string)=>s.replace(/([#$%&_{}])/g,"\\$1");export function renderWidgetLatex(spec:MathWidgetSpec){const checked=validateMathWidget(spec);return`\\begin{quote}\\textbf{${esc(spec.title)}} (${spec.trustLabel})\\par ${esc(checked.textFallback)}\\par Sources: ${spec.sourceEntityIds.map(esc).join(", ")}\\end{quote}`}
