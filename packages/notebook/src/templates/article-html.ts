import type{PublicationModel}from"../publication.ts";const e=(s:string)=>s.replace(/[&<>]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]!));export const articleHtml=(m:PublicationModel,w="")=>`<!doctype html><title>${e(m.title)}</title><h1>${e(m.title)}</h1><strong>${e(w)}</strong>${m.blocks.filter(b=>b.accepted).map((b,i)=>`<h2>${b.kind==="THEOREM"?`Theorem ${i+1}`:e(b.kind)}</h2><p>${e(b.text)}</p>`).join("")}<h2>Claim status</h2>${m.claims.map(c=>`<p>${c.id}: ${c.status}${c.status==="KERNEL_VERIFIED"?"":" — UNVERIFIED"}</p>`).join("")}<h2>Reproducibility</h2><p>Capsule ${e(m.capsuleHash)}</p>`
