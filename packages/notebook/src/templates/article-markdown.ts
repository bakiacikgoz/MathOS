import type{PublicationModel}from"../publication.ts";export const articleMarkdown=(m:PublicationModel,w="")=>`# ${m.title}\n\n${w}\n\n${m.blocks.filter(b=>b.accepted).map((b,i)=>`## ${b.kind==="THEOREM"?`Theorem ${i+1}`:b.kind}\n\n${b.text}`).join("\n\n")}\n\n## Claim status\n${m.claims.map(c=>`- ${c.id}: ${c.status}${c.status==="KERNEL_VERIFIED"?"":" — UNVERIFIED"}`).join("\n")}\n\n## Bibliography\n${m.citations.map(c=>`- [${c.id}] ${c.label}`).join("\n")}\n\n## Reproducibility\nCapsule ${m.capsuleHash}`
