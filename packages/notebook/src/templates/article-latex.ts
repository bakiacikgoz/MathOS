import type{PublicationModel}from"../publication.ts";export const articleLatex=(m:PublicationModel,w="")=>`\\documentclass{article}\\begin{document}\\title{${m.title}}\\maketitle ${w} ${m.blocks.filter(b=>b.accepted).map((b,i)=>`\\section*{${b.kind==="THEOREM"?`Theorem ${i+1}`:b.kind}}${b.text}`).join(" ")} \\section*{Claim status}${m.claims.map(c=>`${c.id}: ${c.status}${c.status==="KERNEL_VERIFIED"?"":" -- UNVERIFIED"}`).join("\\par ")} \\section*{Reproducibility}Capsule ${m.capsuleHash}\\end{document}`
