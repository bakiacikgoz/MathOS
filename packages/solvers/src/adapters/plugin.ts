const guard=(v:any)=>{if(v&&typeof v==="object"&&["claimStatus","verified","kernelVerified"].some(k=>k in v))throw new Error("PLUGIN_AUTHORITY_FIELD_DENIED");return v};export class PluginSolverAdapter{readonly descriptor={id:"plugin",version:"1",health:"READY" as const,problemKinds:["SMT","SYMBOLIC"],maxTrustClass:"UNTRUSTED" as const};constructor(id:string,private readonly call:(request:any)=>Promise<any>){this.descriptor.id=id}async solve(request:any){const r=guard(await this.call(request));if(!["SUPPORT","COUNTEREXAMPLE","UNSAT","SAT","UNKNOWN","ERROR"].includes(r.outcome))throw new Error("PLUGIN_SOLVER_OUTPUT_INVALID");return{...r,trustClass:"UNTRUSTED" as const}}}
