import{describe,expect,test}from"bun:test";import{runAlignmentEval}from"../scripts/run-alignment-eval.ts";describe("alignment benchmark",()=>{test("frozen balanced deterministic harness has trust metrics",()=>{const r=runAlignmentEval();expect(r.caseCount).toBeGreaterThanOrEqual(60);expect(Object.keys(r.perDimension)).toHaveLength(8);expect(r.severeMismatchRecall).toBe(1);expect(r.falseApprovalCandidateRate).toBe(0);expect(r.claim).not.toContain("human equivalent")})})
