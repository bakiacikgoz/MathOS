import{describe,expect,test}from"bun:test";import{ResearchHomeService}from"../packages/core/src/services/research-home-service.ts";describe("research home",()=>{test("reopen after pause/crash excludes noisy events and gives stable next actions",()=>{const service=new ResearchHomeService();const input={objective:{id:"C1",title:"Goal",status:"DRAFT"},events:[{action:"view_opened",createdAt:"3"},{action:"blocker_resolved",target:"B1",createdAt:"2"},{action:"panel_opened",createdAt:"4"}],blockers:[{id:"B2",status:"OPEN"}],criticalPath:["C1","B2"],agenda:[{id:"A2",title:"Second",priority:2},{id:"A1",title:"First",priority:1}],environment:{lean:"PASS"},runStatus:"PAUSED"};const home=service.build(input as any);expect(home.lastMeaningfulProgress?.action).toBe("blocker_resolved");expect(home.nextActions.map(x=>x.id)).toEqual(["A1","A2"]);expect(service.format(home)).toContain("PAUSED")})})
