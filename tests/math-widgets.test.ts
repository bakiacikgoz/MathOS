import{describe,expect,test}from"bun:test";import{validateMathWidget,projectDependencyWidget}from"@mathos/graph";const base={schemaVersion:"math-widget-v1",id:"w1",kind:"NUMERIC_PLOT",title:"Plot",sourceEntityIds:["C1"],trustLabel:"COMPUTATIONAL"};describe("safe math widgets",()=>{test("rejects unknown fields/kinds duplicate IDs scripts URLs and nonfinite numbers",()=>{for(const data of [{...base,kind:"EVIL",data:{}},{...base,data:{points:[[1,NaN]]}},{...base,data:{points:[[1,2]],url:"https://evil"}},{...base,title:"<script>x</script>",data:{points:[]}},{...base,data:{points:[[1,2]],extra:()=>1}}])expect(()=>validateMathWidget(data as any)).toThrow();expect(()=>projectDependencyWidget([{id:"a",label:"A"},{id:"a",label:"B"}],[])).toThrow("WIDGET_DUPLICATE_ID")});test("enforces graph plot and geometry hard limits",()=>{expect(()=>projectDependencyWidget(Array.from({length:2001},(_,i)=>({id:String(i),label:"n"})),[])).toThrow("WIDGET_NODE_LIMIT");expect(()=>validateMathWidget({...base,data:{points:Array.from({length:10001},(_,i)=>[i,i])}} as any)).toThrow("WIDGET_POINT_LIMIT");expect(()=>validateMathWidget({...base,kind:"GEOMETRY_SCENE",data:{primitives:Array.from({length:1001},()=>({type:"segment",from:[0,0],to:[1,1]}))}} as any)).toThrow("WIDGET_PRIMITIVE_LIMIT")});test("accepts bounded deterministic specs with textual fallback",()=>{const result=validateMathWidget({...base,data:{points:[[0,1],[1,2]],series:"s"}} as any);expect(result.textFallback).toContain("2 points");expect(result.spec.trustLabel).toBe("COMPUTATIONAL")})})
