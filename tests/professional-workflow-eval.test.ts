import{describe,expect,test}from"bun:test";import{runProfessionalWorkflowEval}from"../scripts/run-professional-workflow-eval.ts";describe("professional workflow benchmark",()=>{test("runs all deterministic scenarios with failure recovery evidence",()=>{const r=runProfessionalWorkflowEval();expect(r.scenarios).toHaveLength(8);expect(r.scenarios.every(s=>s.status==="PASS"&&s.recovered&&s.evidence.length)).toBe(true);expect(r.trustLabelCorrectness).toBe(1);expect(r.dataLoss).toBe(0);expect(r.recoverability).toBe(1)})})
