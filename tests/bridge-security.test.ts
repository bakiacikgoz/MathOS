import{describe,expect,test}from"bun:test";import{parseBridgeLine,BridgeService}from"../packages/core/src/services/bridge-service.ts";describe("bridge security",()=>{test("payload malformed and DB path denied",async()=>{expect(()=>parseBridgeLine("x")).toThrow("BRIDGE_MALFORMED_JSON");expect(()=>parseBridgeLine(JSON.stringify({x:"a".repeat(1_000_001)}))).toThrow("BRIDGE_PAYLOAD_TOO_LARGE");const b=new BridgeService({workspaceRoot:"C:/w",workspaceId:"W",trusted:false});await expect(b.handle({id:"1",method:"db.path",params:{}})).rejects.toThrow("BRIDGE_METHOD_UNKNOWN");await expect(b.handle({id:"2",method:"claims.create",params:{}})).rejects.toThrow("UNTRUSTED_WORKSPACE_MUTATION")})})
