import { describe,expect,test } from "bun:test"
import { ProofFeedbackLedger,proofFeedbackKey } from "@mathos/retrieval"
describe("proof feedback retrieval evidence",()=>{test("stores immutable classified rows by exact revisions without online learning",()=>{const ledger=new ProofFeedbackLedger(),row={goalHash:"g",setHash:"s",formalRevision:"f",indexRevision:"i",outcome:"UNKNOWN_IDENTIFIER" as const,diagnostics:["unknown identifier x"],createdAt:"now"};ledger.record(row);expect(ledger.list()).toEqual([{...row,key:proofFeedbackKey(row)}]);expect(()=>ledger.record({...row,outcome:"KERNEL_ACCEPTED"})).toThrow("IMMUTABLE_PROOF_FEEDBACK");expect(ledger.runtimeWeights()).toEqual({})});test("classifies compile and proof outcomes",()=>{const ledger=new ProofFeedbackLedger();expect(ledger.classify({compiled:true,kernelAccepted:true,diagnostics:[]})).toBe("KERNEL_ACCEPTED");expect(ledger.classify({compiled:false,kernelAccepted:false,diagnostics:["type mismatch"]})).toBe("TYPE_MISMATCH");expect(ledger.classify({compiled:false,kernelAccepted:false,diagnostics:["unsolved goals"]})).toBe("UNSOLVED_GOALS")})})
