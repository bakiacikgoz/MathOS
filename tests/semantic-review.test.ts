import{describe,expect,test}from"bun:test";import{semanticDiff}from"../packages/core/src/semantic-diff.ts";import{ReviewService}from"../packages/core/src/services/review-service.ts";describe("semantic review",()=>{test("classifies mathematical dimensions",()=>{const diff=semanticDiff({claims:[],formal:"x",sourceLocator:"p1",notebook:"old",verified:true},{claims:[{id:"C"}],formal:"y",sourceLocator:"p2",notebook:"new",verified:false});expect(diff.map(x=>x.dimension)).toEqual(expect.arrayContaining(["CLAIM","FORMAL_STATEMENT","VERIFICATION_STALENESS","SOURCE_CITATION","NOTEBOOK_NARRATIVE"]))});test("canonical packet attestation rejects stale revision",()=>{const s=new ReviewService(()=>"2030");const p=s.createPacket({sourceBranchId:"B1",targetBranchId:"B0",sourceRevision:"r1",targetRevision:"t1",changes:[{dimension:"CLAIM",severity:"MEDIUM",summary:"added"}]});expect(p.markdown).toContain(p.hash);expect(()=>s.attest(p,{reviewer:"alice",decision:"APPROVE",currentSourceRevision:"r2",currentTargetRevision:"t1"})).toThrow("REVIEW_PACKET_STALE");expect(s.attest(p,{reviewer:"alice",decision:"APPROVE",currentSourceRevision:"r1",currentTargetRevision:"t1"}).packetHash).toBe(p.hash)})})
