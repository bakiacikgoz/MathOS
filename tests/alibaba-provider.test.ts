import{describe,expect,test}from"bun:test";import{EnvironmentSecretStore,MODEL_PROFILE_V2_SCHEMA,createProviderFromProfile,evaluateProviderPolicy,providerCatalog,type ModelProfileV2}from"@mathos/models"
function profile(id:string):ModelProfileV2{return{schemaVersion:MODEL_PROFILE_V2_SCHEMA,id,descriptorId:id,displayName:id,model:"qwen-plus",endpointPresetId:null,baseUrlOverride:null,auth:{kind:"secret-ref",secretRef:`model.${id}`},enabled:true,timeoutMs:1000,maxResponseBytes:10000,maxOutputTokens:null,reasoningEffort:null,allowedRoles:["primary"],requestConcurrency:1,metadata:{createdAt:"2026-09-02T00:00:00Z",updatedAt:"2026-09-02T00:00:00Z",migratedFromV1:false}}}
describe("Alibaba providers",()=>{test("blocks plan backends with non-bypassable terms review while allowing PAYG",async()=>{for(const id of["alibaba-token-plan-personal","alibaba-token-plan-team","alibaba-coding-plan"]){expect(providerCatalog.get(id)!.terms.policy).toBe("TERMS_REVIEW_REQUIRED");expect(evaluateProviderPolicy(id).allowed).toBe(false);await expect(createProviderFromProfile(profile(id),{secrets:new EnvironmentSecretStore({})})).rejects.toThrow("PROVIDER_TERMS_RESTRICTED")}expect(evaluateProviderPolicy("alibaba-model-studio-payg").allowed).toBe(true);expect(providerCatalog.get("alibaba-model-studio-payg")!.endpointPresets[0]!.baseUrl).not.toBe(providerCatalog.get("alibaba-coding-plan")!.endpointPresets[0]!.baseUrl)})})
