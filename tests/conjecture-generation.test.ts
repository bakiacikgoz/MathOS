import{describe,expect,test}from"bun:test";import{ConjectureService}from"../packages/core/src/services/conjecture-service.ts";describe("conjecture generation",()=>{test("deterministic generators preserve provenance, remain proposals, dedup and bound output",()=>{const service=new ConjectureService({workspaceId:"W",branchId:"B",contextRevisionId:"CTX-1",maxProposals:3});const rows=service.generateDeterministic({statement:"For every n, f(n)=n",sourceEntityIds:["EXP-1"],missingPrerequisite:"continuity"});expect(rows.length).toBeLessThanOrEqual(3);expect(rows.every(r=>r.status==="PROPOSED"&&r.sourceEntityIds.includes("EXP-1"))).toBe(true);expect(new Set(rows.map(r=>r.naturalStatement.toLowerCase())).size).toBe(rows.length)});test("rejects invalid context and strips model authority",()=>{expect(()=>new ConjectureService({workspaceId:"W",branchId:"B",contextRevisionId:"",maxProposals:2})).toThrow("CONJECTURE_CONTEXT_REQUIRED");const service=new ConjectureService({workspaceId:"W",branchId:"B",contextRevisionId:"C",maxProposals:1});expect(()=>service.generateModel([{naturalStatement:"X",novel:true} as any])).toThrow("FORBIDDEN_AUTHORITY_FIELD");const row=service.generateModel([{naturalStatement:"X",rationale:"r",sourceEntityIds:["S"]}])[0]!;expect(row.status).toBe("PROPOSED");expect(row.createdClaimId).toBeNull()})})
