import{describe,expect,test}from"bun:test";import{AgendaService}from"../packages/core/src/services/agenda-service.ts";describe("research agenda",()=>{test("CRUD revision dependencies cycle and ranking",()=>{const s=new AgendaService(()=>"2030");const a=s.add({kind:"TASK",title:"A",description:"a",priority:2,expectedInformationGain:"HIGH",estimatedCost:"LOW",dependencyIds:[]});const b=s.add({kind:"HUMAN_CHECKPOINT",title:"B",description:"b",priority:1,expectedInformationGain:"MEDIUM",estimatedCost:"LOW",dependencyIds:[a.id]});expect(()=>s.update(a.id,99,{title:"x"})).toThrow("REVISION_CONFLICT");expect(()=>s.update(a.id,1,{dependencyIds:[b.id]})).toThrow("AGENDA_DEPENDENCY_CYCLE");expect(s.next()[0]?.id).toBe(a.id);s.complete(a.id,1);expect(s.next()[0]?.id).toBe(b.id)});test("handoff and bounded agent context treat agenda as state",()=>{const s=new AgendaService(()=>"2030");for(let i=0;i<10;i++)s.add({kind:"TASK",title:`T${i}`,description:"d",priority:i,expectedInformationGain:"LOW",estimatedCost:"HIGH",dependencyIds:[]});expect(s.agentContext(3)).toMatchObject({mode:"RESEARCH_STATE",items:expect.any(Array)});expect(s.agentContext(3).items).toHaveLength(3);expect(s.handoff("R1",["B1"]).nextAgendaItemIds.length).toBeGreaterThan(0)})})
