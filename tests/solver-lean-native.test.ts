import { describe,expect,test } from "bun:test"
import { FakeLeanAdapter } from "@mathos/lean"
import { LeanNativeSolver } from "../packages/solvers/src/adapters/lean-native.ts"
describe("native Lean solver",()=>{test("returns a candidate proof for VerificationGate on tactic success",async()=>{const adapter=new LeanNativeSolver(new FakeLeanAdapter());const result=await adapter.solve({formalDeclaration:"theorem t : True",tactic:"by trivial",workspaceRoot:".",tmpDir:".tmp"});expect(result.outcome).toBe("SUPPORT");expect(result.trustClass).toBe("UNTRUSTED");expect(result.candidateProof).toContain("theorem t : True := by trivial");expect(result.requiresVerificationGate).toBe(true)});test("preserves Lean failure diagnostics without claiming proof",async()=>{const lean=new FakeLeanAdapter();lean.nextProof={result:"ERROR",diagnostics:[{severity:"error",message:"unsolved goals"}],leanVersion:"fake",toolchain:"fake"};const result=await new LeanNativeSolver(lean).solve({formalDeclaration:"theorem t : True",tactic:"by omega",workspaceRoot:".",tmpDir:".tmp"});expect(result.outcome).toBe("ERROR");expect(result.candidateProof).toBeNull();expect(result.diagnostics[0]?.message).toContain("unsolved")})})
