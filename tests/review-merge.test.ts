import{describe,expect,test}from"bun:test";import{governedMergePlan}from"../packages/core/src/services/branch-service.ts";describe("review merge governance",()=>{test("blocks findings stale packets and missing reverify",()=>{expect(governedMergePlan({packetFresh:true,highFindings:1,targetReverified:true,sourceBranch:"b",targetBranch:"main"}).status).toBe("BLOCKED");expect(governedMergePlan({packetFresh:false,highFindings:0,targetReverified:true,sourceBranch:"b",targetBranch:"main"}).status).toBe("BLOCKED");expect(governedMergePlan({packetFresh:true,highFindings:0,targetReverified:false,sourceBranch:"b",targetBranch:"main"}).status).toBe("BLOCKED")});test("returns exact nonautomatic plan",()=>{const p=governedMergePlan({packetFresh:true,highFindings:0,targetReverified:true,sourceBranch:"b",targetBranch:"main"});expect(p.status).toBe("SAFE_TO_APPLY");expect(p.automatic).toBe(false);expect(p.commands[1]).toContain("git merge")})})
