import{describe,expect,test}from"bun:test";import{PublicationService}from"../packages/core/src/services/publication-service.ts";const model={title:"Paper",blocks:[{id:"B1",kind:"THEOREM",text:"Every prime is prime.",accepted:true,claimId:"C1"},{id:"B2",kind:"PROSE",text:"model draft",accepted:false}],claims:[{id:"C1",status:"KERNEL_VERIFIED"},{id:"C2",status:"DRAFT"}],citations:[{id:"R1",label:"Ref"}],references:["C1","R1"],capsuleHash:"cap"};describe("publication service",()=>{test("renders accepted model to three formats with provenance",()=>{const r=new PublicationService().render(model as any);for(const value of Object.values(r.outputs)){expect(value).toContain("Paper");expect(value).not.toContain("model draft")}expect(r.outputs.markdown).toContain("Theorem 1");expect(r.outputs.markdown).toContain("KERNEL_VERIFIED");expect(r.outputs.markdown).toContain("Reproducibility");expect(r.outputs.markdown).toContain("UNVERIFIED")});test("broken references fail or watermark incomplete output",()=>{const bad={...model,references:["MISSING"]};expect(()=>new PublicationService().render(bad as any)).toThrow("PUBLICATION_BROKEN_REFERENCE");const r=new PublicationService().render(bad as any,{allowIncomplete:true});expect(r.lossReport.length).toBeGreaterThan(0);expect(r.outputs.html).toContain("INCOMPLETE PUBLICATION")})})
