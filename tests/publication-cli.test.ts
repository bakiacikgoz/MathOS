import{describe,expect,test}from"bun:test";import{publicationCommand,publicationSnapshot}from"../apps/tui/src/ui/PublicationViews.tsx";describe("publication CLI",()=>{test("build inspect and incomplete watermark",()=>{expect(publicationCommand(["build"]).action).toBe("build");expect(publicationCommand(["inspect"]).action).toBe("inspect");expect(publicationSnapshot({sections:["theorems"],lossReport:["BROKEN_REFERENCE"]}).watermark).toBe("INCOMPLETE PUBLICATION")})})
