import{describe,expect,test}from"bun:test";import{reviewCommand,reviewSnapshot}from"../apps/tui/src/ui/ReviewViews.tsx";describe("review CLI",()=>{test("packet findings attest visible hash",()=>{for(const x of ["create","finding-add","finding-resolve","attest"])expect(reviewCommand([x]).action).toBe(x);expect(reviewSnapshot({hash:"abc",findings:[],status:"OPEN"}).hash).toBe("abc")})})
