# Product scale benchmark

`current.json` is generated by `scripts/product-scale-benchmark.ts`. The frozen
fixture contains 10,000 claims, 9,999 dependency edges, and 100,000 metadata-only
events. Release qualification fails if Atlas projection or event serialization
exceeds two seconds on the qualification host.
